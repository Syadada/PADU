# =====================================================================
# Python Requirements for PADU Analytics Engine (DTSEN 2026)
# =====================================================================
# Python Version: 3.8+ (Python 3.11+ Recommended)
# 
# Catatan: Seluruh pustaka utama yang digunakan (sqlite3, csv, json, time, 
# datetime, os, sys) adalah PUSTAKA STANDAR (built-in) dari Python.
# Tidak diperlukan instalasi `pip install` tambahan untuk fungsi utama.
# =====================================================================

# Pustaka opsional untuk akselerasi impor & membaca file Excel (.xlsx / .csv):
pandas>=2.0.0
pyarrow>=14.0.0
openpyxl>=3.1.0
